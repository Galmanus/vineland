# AXL

This page has moved. AXL is documented under [`docs/axl/`](axl/README.md):

- [`docs/axl/README.md`](axl/README.md) — what AXL is, the four directives
  (bind / constrain / prove / invariant), a worked example, and the relation to
  the smart-wallet's sliding-window bound.
- [`docs/axl/compiler.md`](axl/compiler.md) — the `axlc` CLI, SMT-LIB lowering,
  decision procedure, and certificate shape.
- [`docs/axl/proofs-and-limits.md`](axl/proofs-and-limits.md) — exactly what is
  proved (the `2 * window_cap` bound), the proof-carrying certificate, and the
  honest limitations (build-only, no on-chain artifact, no required CI gate, zero
  downstream adoption).
