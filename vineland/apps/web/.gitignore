.hero-src/

# Env files — NEVER commit (audit L4). VITE_-prefixed values get inlined into
# the world-readable bundle. Ignore all .env*, keep *.example templates tracked.
.env
.env.*
!.env.example
!.env.*.example
